# Atlassian Connect App using Express & Typescript

To run the application in developer mode directly without compiling the code into the javascript, run the command
```
npm run dev
```

To generate the equivalent javascript files 
```
npm run build
```

To run the actual server using javascript code
```
npm start
```